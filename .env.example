# Base API URL (without version path — /v4/test is appended automatically)

# Development — uses Vite proxy defined in vite.config.ts:
VITE_API_BASE_URL=/c300/api

# Production — direct URL:
# VITE_API_BASE_URL=https://showroom.eis24.me/c300/api
